Script installs all external dependencies for this project:
    pip install pandas scikit-learn cryptography seaborn matplotlib numpy

venv creation:
    python3 -m venv .venv

venv activation:
    source .venv/bin/activate

DL library installs:
    pip install pandas numpy matplotlib seaborn scikit-learn tensorflow joblib

ML library installs:
    pip install pandas scikit-learn matplotlib seaborn